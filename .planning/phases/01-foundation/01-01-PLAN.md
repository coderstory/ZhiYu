---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - build.gradle.kts
  - settings.gradle.kts
  - gradle.properties
  - gradle/wrapper/gradle-wrapper.properties
  - app/build.gradle.kts
  - app/proguard-rules.pro
  - app/src/main/AndroidManifest.xml
  - app/src/main/res/values/strings.xml
  - app/src/main/res/values/colors.xml
  - app/src/main/res/values/themes.xml
autonomous: true
requirements: [FND-01, FND-02]
user_setup:
  - service: Android SDK
    why: Required to compile and run the project; API 36 + Build Tools 36.0.0
    env_vars:
      - name: ANDROID_HOME
        source: Install Android Studio -> SDK Manager -> SDK Platform 36
    dashboard_config:
      - task: Install Android SDK Platform 36 and Build Tools 36.0.0
        location: Android Studio -> SDK Manager or `sdkmanager "platforms;android-36" "build-tools;36.0.0"`
  - service: Gradle
    why: Required for `gradle wrapper` command to generate wrapper scripts
    install_command: sdk install gradle 9.3.1 (SDKMAN) or use package manager; alternatively download from https://services.gradle.org/distributions/gradle-9.3.1-bin.zip
    note: Only needed once; after wrapper generation, project uses gradlew

must_haves:
  truths:
    - "Project compiles with AGP 9.0.28, Kotlin 2.3.21, Compose BOM 2026.04.01 via version catalog"
    - "Android 16 (API 36) is set as both minSdk and targetSdk"
    - "Application ID com.zhiyu.app is configured in manifest and build config"
    - "Splash theme Theme.ZhiYu.Splash is defined with correct parent and attributes"
    - "All Phase 2-6 library versions (Navigation Compose, compose-richtext) declared in catalog"
  artifacts:
    - path: gradle/libs.versions.toml
      provides: Single-source version catalog for all Phase 1-6 dependencies
      contains: agp = "9.0.28"
    - path: app/build.gradle.kts
      provides: Module-level build config with all plugins and dependencies
      contains: compileSdk = 36
    - path: app/src/main/res/values/themes.xml
      provides: Splash screen theme and base app theme
      contains: Theme.SplashScreen
  key_links:
    - from: app/build.gradle.kts
      to: gradle/libs.versions.toml
      via: alias(libs.*) references
      pattern: "libs\\."
    - from: AndroidManifest.xml
      to: themes.xml
      via: android:theme="@style/Theme.ZhiYu.Splash"
      pattern: "Theme\\.ZhiYu"
    - from: app/build.gradle.kts
      to: settings.gradle.kts
      via: module include and plugin resolution
      pattern: "app"
---

<objective>
Establish the complete Gradle build system, version catalog, and Android resource scaffold for the ZhiYu (知屿) project.

**Purpose:** Every downstream plan depends on this build system. Defines all library versions, Gradle plugins, Android SDK targets, and base Android resources (manifest, themes, strings, colors).

**Output:** Version catalog (`gradle/libs.versions.toml`), root and module Gradle configs, Gradle wrapper, Android manifest, base resource XML files, proguard rules.

**Kotlin version note per MIUIX requirement:** Use Kotlin 2.3.21 (NOT 2.3.20 from CLAUDE.md) per RESEARCH.md finding that MIUIX v0.9.1 requires 2.3.21.
</objective>

<execution_context>
Task automation is fully autonomous. No checkpoints.
</execution_context>

<context>
@CLAUDE.md — Project instructions, tech stack constraints
@.planning/ROADMAP.md — Phase 1 requirements FND-01, FND-02
@.planning/phases/01-foundation/01-RESEARCH.md — Standard Stack section, version catalog, pitfalls
@.planning/phases/01-foundation/01-CONTEXT.md — Decisions D-11 (app ID), D-12 (display name), D-13 (project name), D-08/D-09/D-10 (splash)

<interfaces>
No prior codebase exists. This is greenfield. The build system created here defines the dependency coordinates that all subsequent plans must use via `gradle/libs.versions.toml`.

**Contract for downstream plans:** Every Kotlin source file in Plans 2-4 must import dependencies through `libs.*` catalog references. No hardcoded version strings in any build.gradle.kts beyond what is in the catalog.

**Key versions enforced:**
- AGP 9.0.28, Kotlin 2.3.21, Gradle 9.3.1, KSP 2.3.6
- Compose BOM 2026.04.01 (manages ALL Compose artifacts)
- Room 2.8.4, DataStore Preferences 1.2.1, Koin 4.2.1, MIUIX 0.9.1
- Core SplashScreen 1.2.0, Navigation Compose 2.9.8
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create version catalog and root Gradle configuration</name>
  <files>
    gradle/libs.versions.toml
    build.gradle.kts
    settings.gradle.kts
    gradle.properties
    gradle/wrapper/gradle-wrapper.properties
  </files>
  <action>
    Create the following files for the project root:

    **1. `gradle/libs.versions.toml`** — Complete version catalog with all Phase 1-6 dependencies.

    Versions block (alphabetical):
    - agp = "9.0.28"
    - kotlin = "2.3.21"
    - ksp = "2.3.6"
    - compose-bom = "2026.04.01"
    - room = "2.8.4"
    - navigation-compose = "2.9.8"
    - datastore-preferences = "1.2.1"
    - core-splashscreen = "1.2.0"
    - koin = "4.2.1"
    - miuix = "0.9.1"
    - compose-richtext = "1.0.0-alpha03"

    Libraries block (alphabetical by accessor name, NOT group):
    - compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "compose-bom" }
    - compose-foundation = { group = "androidx.compose.foundation", name = "foundation" }
    - compose-material-icons-extended = { group = "androidx.compose.material", name = "material-icons-extended" }
    - compose-material3 = { group = "androidx.compose.material3", name = "material3" }
    - compose-ui = { group = "androidx.compose.ui", name = "ui" }
    - compose-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }
    - compose-ui-test-junit4 = { group = "androidx.compose.ui", name = "ui-test-junit4" }
    - compose-ui-test-manifest = { group = "androidx.compose.ui", name = "ui-test-manifest" }
    - compose-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
    - compose-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
    - core-splashscreen = { group = "androidx.core", name = "core-splashscreen", version.ref = "core-splashscreen" }
    - datastore-preferences = { group = "androidx.datastore", name = "datastore-preferences", version.ref = "datastore-preferences" }
    - koin-android = { group = "io.insert-koin", name = "koin-android", version.ref = "koin" }
    - koin-androidx-compose = { group = "io.insert-koin", name = "koin-androidx-compose", version.ref = "koin" }
    - miuix-ui = { group = "top.yukonga.miuix.kmp", name = "miuix-ui", version.ref = "miuix" }
    - navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigation-compose" }
    - richtext-ui = { group = "com.halilibo.compose-richtext", name = "richtext-ui", version.ref = "compose-richtext" }
    - richtext-commonmark = { group = "com.halilibo.compose-richtext", name = "richtext-commonmark", version.ref = "compose-richtext" }
    - richtext-ui-material3 = { group = "com.halilibo.compose-richtext", name = "richtext-ui-material3", version.ref = "compose-richtext" }
    - room-compiler = { group = "androidx.room", name = "room-compiler", version.ref = "room" }
    - room-ktx = { group = "androidx.room", name = "room-ktx", version.ref = "room" }
    - room-runtime = { group = "androidx.room", name = "room-runtime", version.ref = "room" }
    - room-testing = { group = "androidx.room", name = "room-testing", version.ref = "room" }

    Plugins block:
    - android-application = { id = "com.android.application", version.ref = "agp" }
    - kotlin-compose-compiler = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
    - ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }

    IMPORTANT: Do NOT include `kotlin-android` plugin. AGP 9.0+ has built-in Kotlin support.
    IMPORTANT: Do NOT include `androidx.compose.compiler:compiler` — Compose compiler is built into Kotlin 2.0+.

    **2. `build.gradle.kts`** (root) — Minimal, only declares plugin aliases with `apply false`:
    - com.android.application
    - org.jetbrains.kotlin.plugin.compose
    - com.google.devtools.ksp

    **3. `settings.gradle.kts`** — Standard project settings:
    - pluginManagement with repositories: google(), mavenCentral(), gradlePluginPortal()
    - dependencyResolutionManagement with repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) and repositories google(), mavenCentral()
    - rootProject.name = "ZhiYu" (per D-13)
    - include(":app")

    **4. `gradle.properties`** — Standard Android Gradle properties:
    - org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
    - android.useAndroidX=true
    - kotlin.code.style=official
    - android.nonTransitiveRClass=true

    **5. `gradle/wrapper/gradle-wrapper.properties`** — Configure for Gradle 9.3.1:
    - distributionUrl=https\://services.gradle.org/distributions/gradle-9.3.1-bin.zip
    - distributionType=bin
    - zipStoreBase=GRADLE_USER_HOME
    - zipStorePath=wrapper/dists

    After creating these files, generate the Gradle wrapper scripts:
    - If Gradle 9.3.1 is available on PATH: `gradle wrapper --gradle-version 9.3.1`
    - If not available: install temporarily via `sdk install gradle 9.3.1` (SDKMAN) or `brew install gradle` (macOS)
    - If SDKMAN/homebrew unavailable: manually download gradle-wrapper.jar into gradle/wrapper/ from the Gradle distribution zip
    - The wrapper batch/shell scripts and jar file must end up in gradle/wrapper/ so that `./gradlew` works
  </action>
  <verify>
    <automated>cat gradle/libs.versions.toml | grep -c "agp = \"9.0.28\"" | xargs test 1 -eq</automated>
  </verify>
  <done>
    Version catalog created with all Phase 1-6 dependencies. Root Gradle config files created. Gradle wrapper generates successfully (`./gradlew --version` produces output).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create app module build configuration</name>
  <files>
    app/build.gradle.kts
    app/proguard-rules.pro
  </files>
  <action>
    Create the app module build configuration:

    **1. `app/build.gradle.kts`** — Module-level build config:

    plugins block (exactly three):
    - alias(libs.plugins.android.application)
    - alias(libs.plugins.kotlin.compose.compiler)
    - alias(libs.plugins.ksp)

    android block:
    - namespace = "com.zhiyu.app"
    - compileSdk = 36
    - defaultConfig: applicationId = "com.zhiyu.app" (D-11), minSdk = 36 (D-02), targetSdk = 36 (D-02), versionCode = 1, versionName = "1.0.0"
    - buildTypes.release: isMinifyEnabled = false (Phase 6 enables it)
    - compileOptions: sourceCompatibility = JavaVersion.VERSION_17, targetCompatibility = JavaVersion.VERSION_17
    - kotlinOptions: jvmTarget = "17"
    - buildFeatures: compose = true

    dependencies block (all via libs.* catalog references):
    - val composeBom = platform(libs.compose.bom); implementation(composeBom)
    - implementation(libs.compose.ui), implementation(libs.compose.ui.graphics), implementation(libs.compose.ui.tooling.preview)
    - implementation(libs.compose.material3), implementation(libs.compose.material.icons.extended), implementation(libs.compose.foundation)
    - debugImplementation(libs.compose.ui.tooling), debugImplementation(libs.compose.ui.test.manifest)
    - implementation(libs.room.runtime), implementation(libs.room.ktx), ksp(libs.room.compiler)
    - implementation(libs.datastore.preferences)
    - implementation(libs.core.splashscreen)
    - implementation(libs.koin.android), implementation(libs.koin.androidx.compose)
    - implementation(libs.miuix.ui)
    - testImplementation(libs.compose.ui.test.junit4)
    - androidTestImplementation(libs.compose.ui.test.junit4)

    **2. `app/proguard-rules.pro`** — Standard Android proguard rules:
    - Keep Koin: `-keepclassmembers class * { @org.koin.core.annotation.Single *; }`
    - Keep Room: `-keep class * extends androidx.room.RoomDatabase { *; }`
    - Keep Compose: `-keep class androidx.compose.** { *; }`

    **Important pitfalls to avoid:**
    - Do NOT apply `org.jetbrains.kotlin.android` plugin — AGP 9.0+ has built-in Kotlin support; applying it causes build failure.
    - Do NOT add `androidx.compose.compiler:compiler` — Compose compiler is built into Kotlin 2.0+. Use `org.jetbrains.kotlin.plugin.compose` plugin only.
    - Do NOT use KAPT — KSP replaces KAPT for all annotation processing. Only `ksp()` for room-compiler.
    - Do NOT set `exclude` or `pickFirsts` for META-INF files unless build fails verify below.
  </action>
  <verify>
    <automated>cat app/build.gradle.kts | grep -c "com.zhiyu.app" | xargs test 0 -lt</automated>
  </verify>
  <done>
    app/build.gradle.kts compiles all Phase 1 dependencies via version catalog references. ProGuard rules file created.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create AndroidManifest, resource XMLs, and base splash theme</name>
  <files>
    app/src/main/AndroidManifest.xml
    app/src/main/res/values/strings.xml
    app/src/main/res/values/colors.xml
    app/src/main/res/values/themes.xml
  </files>
  <action>
    Create the Android resource files for the app:

    **1. `app/src/main/AndroidManifest.xml`**:
    - package="com.zhiyu.app" (legacy attribute for manifest merger, modern AGP uses namespace from build.gradle.kts)
    - application: android:label="@string/app_name" (D-12: "知屿"), android:icon="@mipmap/ic_launcher"
    - android:theme="@style/Theme.ZhiYu.Splash" — splash theme is the ENTRY theme
    - Single Activity declaration: `android:name=".MainActivity"`, `android:exported="true"`
    - Intent filter: ACTION_MAIN + CATEGORY_LAUNCHER
    - No other activities, services, or receivers in Phase 1

    **2. `app/src/main/res/values/strings.xml`**:
    - `<string name="app_name">知屿</string>` (D-12)

    **3. `app/src/main/res/values/colors.xml`**:
    - `<color name="miui_warm_orange">#FF6B35</color>` — MIUI accent primary
    - `<color name="splash_background">#FF6B35</color>` — splash background (reuse for clarity)
    - `<color name="white">#FFFFFF</color>`

    **4. `app/src/main/res/values/themes.xml`**:
    - Define Theme.ZhiYu.Splash with parent="Theme.SplashScreen":
      - `<item name="windowSplashScreenBackground">@color/splash_background</item>`
      - `<item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_adaptive_foreground</item>`
      - `<item name="postSplashScreenTheme">@style/Theme.ZhiYu</item>`
    - Define Theme.ZhiYu with parent="Theme.Material3.DayNight.NoActionBar":
      - Minimal — just empty style body. The Compose layer (ZhiYuTheme) handles all visual theming.
      - `<item name="android:statusBarColor">@android:color/transparent</item>`
      - `<item name="android:navigationBarColor">@android:color/transparent</item>`
    - Also add Theme.ZhiYu as a postSplashScreenTheme target with android:windowBackground null or transparent to prevent white flash after splash
  </action>
  <verify>
    <automated>cat app/src/main/AndroidManifest.xml | grep -c "com.zhiyu.app" | xargs test 0 -lt</automated>
  </verify>
  <done>
    AndroidManifest declares com.zhiyu.app with splash theme as entry point. Themes.xml defines splash theme with #FF6B35 background, adaptive icon drawable, and post-splash theme. Strings and colors resources created.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Version catalog (TOML) -> build.gradle.kts | Version references cross from declarative TOML into imperative Gradle DSL |
| Manifest -> Android system | Theme reference in manifest must resolve to a defined style resource |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-FND-01 | Tampering | gradle/libs.versions.toml | accept | Version catalog is local-only, no external contribution. All versions pinned, no dynamic ranges. |
| T-FND-02 | Information Disclosure | themes.xml postSplashScreenTheme reference | mitigate | Verify on first build that the `postSplashScreenTheme="@style/Theme.ZhiYu"` reference actually resolves. Unresolved theme causes build error at compile time. |
| T-FND-SC | Tampering | Maven Central / Google Maven packages | mitigate | All packages verified in RESEARCH.md Package Legitimacy Audit. All from Google Maven or Maven Central with pinned versions. No [SUS] or [SLOP] packages in phase scope. |
</threat_model>

<verification>
1. Run `./gradlew --version` — confirms Gradle 9.3.1 wrapper works
2. Run `./gradlew :app:assembleDebug` — MUST SUCCEED (full compilation with all source files from Plans 2-4). If this runs immediately after Plan 1 (before Plan 2-4 files exist), it will fail — this is expected. Full verification after all Wave 2 plans complete.
3. Automated checks:
   - `cat gradle/libs.versions.toml | grep -c "agp = \"9.0.28\""` — version catalog has AGP
   - `cat app/build.gradle.kts | grep -c "com.zhiyu.app"` — app config correct
   - `cat app/src/main/AndroidManifest.xml | grep -c "MainActivity"` — manifest has activity
   - `cat app/src/main/res/values/themes.xml | grep -c "Theme.SplashScreen"` — splash theme exists
</verification>

<success_criteria>
- [ ] Version catalog at `gradle/libs.versions.toml` contains all Phase 1-6 dependencies
- [ ] app/build.gradle.kts references exactly 3 plugins (no kotlin-android, no compose-compiler artifact)
- [ ] minSdk = targetSdk = compileSdk = 36 in app/build.gradle.kts
- [ ] AndroidManifest declares com.zhiyu.app application ID and splash theme as entry
- [ ] themes.xml defines both Theme.ZhiYu.Splash (parent Theme.SplashScreen) and Theme.ZhiYu (parent Material3.DayNight.NoActionBar)
- [ ] Gradle wrapper configured for Gradle 9.3.1
</success_criteria>

<output>
Plan complete. Execute via `/gsd:execute-phase 01-foundation --plan 01-01`
</output>
